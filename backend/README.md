# backend

Mini App ↔ server API client layer။

| Path | အဓိပ္ပာယ် |
|------|-----------|
| `api/client.ts` | Vultr API — orders, screenshot submit |

Server implementation: project root `server/` (Express + PostgreSQL + Telegram bot).
